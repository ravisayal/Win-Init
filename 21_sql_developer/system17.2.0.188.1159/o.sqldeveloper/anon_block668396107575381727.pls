DECLARE
  P_UPLOADED_BY VARCHAR2(200);
  P_FILE_TYPE VARCHAR2(200);
BEGIN
  P_UPLOADED_BY := NULL;
  P_FILE_TYPE := NULL;

  HUBS_UTILITY_PKG.HUBS_FILE_LOAD_TO_UNIX(
    P_UPLOADED_BY => P_UPLOADED_BY,
    P_FILE_TYPE => P_FILE_TYPE
  );
--rollback; 
END;
